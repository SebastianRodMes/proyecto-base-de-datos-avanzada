# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Configuracion local (no subir credenciales de maquina)
05-etl/.env

# Archivos intermedios del ETL (bcp)
*.dat

# Logs del ETL cloud automatico (run-etl-cloud.ps1)
05-etl/logs/

# Sistema operativo
Thumbs.db
.DS_Store
desktop.ini

# Power BI: cache y ajustes locales (no versionar)
.pbi/
*.abf
*.pbix
.secrets

# Credenciales locales de la migracion cloud (NUNCA versionar)
.secrets/
.secrets*
05-etl/.env.aws
05-etl/.env.*.bak
*_accessKeys.csv
*.pem

# Archivos temporales de Office
~$*
